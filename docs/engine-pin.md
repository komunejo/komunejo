# Engine pin

The bundled engine at `skills/entity-management/` is an exact copy of a tagged upstream release (DEC-018), never forked or edited here (DEC-001).

| | |
|---|---|
| Upstream | `github.com/komunejo/entity-management` |
| Pinned tag | `v0.4.0` |
| Skill version | 0.4.0 |
| Vendored | 2026-07-12, via `git archive v0.4.0 entity-management` |

Updating the copy is a deliberate act: vendor the new tag the same way, update this table, and record the change in the repository's history. Never a silent refresh toward "latest." Improvements to the engine go upstream first.
